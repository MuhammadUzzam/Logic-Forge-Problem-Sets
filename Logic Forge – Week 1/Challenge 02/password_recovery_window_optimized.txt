We use a sliding window with two pointers.
The right pointer expands the window, and the left pointer shrinks it
when all pattern characters are matched.
This avoids repeated work and finds the minimum window efficiently.

Time Complexity: O(n)
Space Complexity: O(k)